---
// import projectImage from "@assets/placeholders/project-placeholder-image.png";
import { Image } from "astro:assets";
import ArrowRight from "@assets/icons/ArrowRight.astro";
import type { ProjectType } from "src/types/projects-types";

const { title, description, tags, projectRepo, projectUrl, projectImage } = Astro.props as ProjectType;
console.log(title);
---

<article class="w-full">
  <figure class="w-full aspect-video">
    <Image
      class="w-full min-h-56 h-full max-h-80 object-center object-cover"
      src={`http://localhost:8000${projectImage?.url}`}
      width={750}
      height={640}
      alt="Screenshot of The Munch Hut project"
    />
  </figure>

  <div class="mt-4">
    <div class="w-full flex flex-wrap gap-2">
      {
        tags.map(({ category }) => {
          return (
            <p class="text-[10px] uppercase tracking-wide px-3 py-1.5 bg-white/30 inline rounded-4xl cursor-pointer">
              {category}
            </p>
          );
        })
      }
    </div>

    <h2 class="text-lg font-semibold mt-3">{title}</h2>

    <p class="text-sm mt-2 line-clamp-4">
      {description}
    </p>

    <div class="w-full mt-5 flex justify-start items-center gap-6 text-sm">
      <a
        class="inline-flex justify-center items-center gap-1 py-1"
        href={projectRepo}
      >
        Repo
        <span class="mt-1">
          <ArrowRight />
        </span>
      </a>
      <a
        href={projectUrl}
        class="bg-orange py-[6px] px-4 rounded-md hover:bg-orange-shade"
      >
        Live Project
      </a>
    </div>
  </div>
</article>
